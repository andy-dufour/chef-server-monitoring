// Statsd configuration
{
  graphite: {
    legacyNamespace: false,
  },
  graphitePort: <%= node['cs_statsd']['graphite_port'] %>,
  graphiteHost: "<%= node['cs_statsd']['graphite_host'] %>",
  port: 8125,
  // Debug
  //backends: [ "./backends/graphite", "./backends/console" ],
  // Graphite
  backends: [ "./backends/graphite" ],
  servers: [
    { server: "./servers/tcp" },
    { server: "./servers/udp" }
  ]
}
